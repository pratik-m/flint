/* Obsidian-like Style - Adapted for Rich Markdown Rendering */

/* Note: Rich rendering uses a single Static widget, so we can only
   style the container. Individual element styling (headers, blockquotes)
   is handled by Rich's renderer and cannot be customized via TCSS. */

.style-obsidian CustomMarkdownViewer {
    padding: 1 4;
    background: $surface;
}

.style-obsidian Static {
    padding: 2;
    background: transparent;
}
